# Local environment (keep production .env on Hostinger only)
.env
.env.*
!.env.example

# Runtime logs
error_log
**/error_log
*.log

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# FTP deploy state (if used later)
.ftp-deploy-sync-state.json
